spring:
  application:
    name: portfolio
  datasource:
    url: jdbc:postgresql://ep-flat-river-ayjau108-pooler.c-5.us-east-2.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_nSYfqiATlH51
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false
    clean-on-validation-error: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 15MB
      max-request-size: 15MB
  web:
    cors:
      allowed-origins: ${CORS_ORIGINS:http://localhost:3000}

server:
  port: 8080
  servlet:
    context-path: /api

app:
  admin:
    secret-key: portfolio-admin-2024
  upload:
    dir: ./uploads
    public-path: /api/uploads

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
